# Server Configuration
PORT=5001
NODE_ENV=development
# Comma-separated allowed, e.g. https://your-frontend.onrender.com,https://www.yourdomain.com
CLIENT_URL=http://localhost:3000

# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/creatorconnect

# JWT Secret (generate a strong random string for production)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Email Configuration
# Option A (recommended for Render): Generic SMTP provider (Brevo/SendGrid/Mailgun/etc.)
# SMTP_HOST=smtp-relay.brevo.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# EMAIL_FROM=no-reply@yourdomain.com
#
# Option B (backward-compatible): Gmail App Password
# For Gmail, you need to use an App Password, not your regular password:
# https://support.google.com/accounts/answer/185833
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-character-app-password

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Cashfree Payment Gateway Configuration
CASHFREE_APP_ID=your-cashfree-app-id
CASHFREE_SECRET_KEY=your-cashfree-secret-key
# sandbox | production
CASHFREE_ENV=sandbox
# Dev helper: auto-mark token payment as successful without real payment.
# true only for local testing; keep false in production.
CASHFREE_AUTO_SUCCESS=false
# Keep in sync with Cashfree API docs for your account.
CASHFREE_API_VERSION=2023-08-01
CASHFREE_WEBHOOK_TOLERANCE_SEC=300

# Optional: explicit frontend return URL after checkout
# TOKEN_BUY_RETURN_URL=http://localhost:3000/tokens/buy
